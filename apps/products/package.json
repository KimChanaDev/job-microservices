{
  "name": "@job-microservices/products",
  "version": "0.0.1",
  "description": "Products microservice",
  "main": "dist/main.js",
  "scripts": {
    "drizzle-kit:generate": "dotenv -e ../../.env -- npx drizzle-kit generate --config drizzle.config.ts --name=%MIGRATION_NAME% && node drizzle/scripts/fix-migrations.mjs",
    "drizzle-kit:migrate": "dotenv -e ../../.env -- npx drizzle-kit migrate --config drizzle.config.ts",
    "drizzle-kit:migrate:prod": "npx drizzle-kit migrate --config drizzle.config.ts",
    "drizzle-kit:full-migrate": "dotenv -e ../../.env -- npx drizzle-kit generate --config drizzle.config.ts --name=%MIGRATION_NAME% && node drizzle/scripts/fix-migrations.mjs && dotenv -e ../../.env -- npx drizzle-kit migrate --config drizzle.config.ts",
    "drizzle-kit:custom": "npx drizzle-kit generate --custom --name",
    "_example(drizzle-kit:custom)": "npm run drizzle-kit:custom --workspace='apps/products' -- seed-categories"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.0.1",
    "drizzle-orm": "^0.44.5",
    "pg": "^8.16.3",
    "@nestjs/microservices": "^11.1.6",
    "drizzle-kit": "^0.31.4"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
