{
  "name": "@job-microservices/auth",
  "version": "0.0.1",
  "description": "Authentication microservice",
  "main": "dist/main.js",
  "scripts": {
    "prisma:generate": "dotenv -e ../../.env -- prisma generate --schema ./prisma/schema.prisma",
    "prisma:migrate": "dotenv -e ../../.env -- prisma migrate dev --name users_table --schema ./prisma/schema.prisma"
  },
  "dependencies": {
    "@graphql-yoga/nestjs": "^3.15.2",
    "@nestjs/config": "^4.0.2",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/passport": "^11.0.5",
    "bcryptjs": "^3.0.2",
    "passport-jwt": "^4.0.1"
  }
}
